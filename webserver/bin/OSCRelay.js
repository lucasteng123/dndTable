// Generated by CoffeeScript 2.3.1
(function() {
  // External packages
  var OSC, OSCRelay;

  OSC = require('osc-js');

  OSCRelay = class OSCRelay {
    constructor(from, to, paths) {
      this.from = from;
      this.to = to;
      this.paths = paths;
    }

    bind() {
      var i, len, path, ref, results, self;
      self = this;
      ref = this.paths;
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        path = ref[i];
        results.push((function() {
          var thisPath;
          thisPath = path; // fucking lexical scoping
          return self.from.on(path, function(message) {
            console.log(thisPath);
            return self.to.send(new OSC.Message(thisPath, ...message.args));
          });
        })());
      }
      return results;
    }

  };

  module.exports = OSCRelay;

}).call(this);
